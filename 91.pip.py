# pip = python module installer (package manager for package and modules from python package index)

# include for python vesions 3.4+
# open command prompt

# help:                     pip
# check:                    pip --version
# update:                   pip install --upgrade pip
# installed packages:       pip list
# check outdated packages:  pip list--outdated
# install a package:        pip install "package name"